@model IEnumerable<MovApp.Models.MovieCreatorProfession>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Creator.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Movie.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Profession.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Creator.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Movie.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Profession.Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MovieCreatorProfessionId }) |
            @Html.ActionLink("Details", "Details", new { id=item.MovieCreatorProfessionId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MovieCreatorProfessionId })
        </td>
    </tr>
}

</table>
